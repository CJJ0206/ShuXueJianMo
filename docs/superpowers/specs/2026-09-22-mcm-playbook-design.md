# 研究生数学建模竞赛 参赛作战包 —— 设计文档

日期：2026-09-22　状态：待评审
适用范围：中国研究生数学建模竞赛（3 人队，AI 主力执行）
本文档定义**交付物**，不定义任何具体赛题的解法。2025 年 A–F 六题只作为流程夹具。

---

## 1. 目标与非目标

### 目标
产出一套明天开赛即可上场的作战包，含四件东西：

1. **《执行规范（我侧）》** —— 开赛前 36 小时内，用户 + AI 如何产出"全题初版方案"。
2. **《验收规范（全队）》** —— 用可机器判定的 V0–V3 四级验收，判定初版能否交接、终稿能否提交。
3. **prompt 卡片库**（30 张）—— 每张卡对应流程里一个动作，含变量占位、必落文件、人工判定项。
4. **工程脚手架** —— 目录模板 + 单一事实源文件 + 自动检查脚本。

### 非目标
- **不求解任何一道题。**今晚与明天都不产出题目答案。
- 不做具体模型的选型（模型选择是明天按当天赛题现场决定的事）。
- 不搭建 Qoder Skill / 不做自动化编排。全流程靠人复制粘贴 prompt 驱动——依赖越少，赛场故障模式越少。

---

## 2. 已确认的前提事实

### 2.1 队伍与工作方式
- 3 人队，**AI 为主力执行，三名队员均为 AI-first 工作方式**。
- 赛制：约 4 天，2026-09-23 开赛（本文所有时间盒以此为基准，可参数化调整）。
- 分工模型 = **两班制**：前 1.5 天用户 + AI 冲刺出全题初版，D1.5 交接，后 2.5 天用户退出主力位转任编排/仲裁。

### 2.2 环境（2026-09-22 实测）
| 项 | 事实 | 影响 |
|---|---|---|
| Python | `F:\anaconda\envs\ptg\python.exe`，3.10.16 | 所有脚本用**全路径**或先 `conda activate ptg` |
| PATH 上的 `python` | WindowsApps 空壳，执行即报"未找到" | **头号环境坑**，写进 SETUP 首行 |
| 已装 | numpy 2.0.2 / pandas 2.3.2 / scipy 1.13.1 / matplotlib 3.10.3 / sklearn 1.7.1 / torch 2.9.0+cu128 / cv2 4.13 / seaborn / networkx 3.4.2 / xgboost 3.1.3 / openpyxl / sympy | 通用建模、DL、图算法可直接用 |
| 未装 | numba, pywt, statsmodels, shap, lightgbm, python-docx, pulp, ortools, deap, ezdxf, pdfplumber, xarray, netCDF4, geopandas | 今晚补齐（见 §9） |
| GPU | RTX 5060 Ti 16 GB，CUDA 12.8 可用 | 可训中等模型 |
| LaTeX / MATLAB / R | **均无** | 论文只能走 Word/WPS 路线；公式在 Word 里排版 |
| 网络 | 国内镜像通（清华源 / PyPI / ModelScope）；GitHub 超时 | 依赖走国内镜像；附件下载需现场探路 |
| 本地数据 | 工作目录**只有六道题题面**（PDF + DOCX），**无任何附件** | 附件必须开赛当天下载，故 §8 把"下载+体检"排在最前 |

### 2.3 论文格式基线
无 LaTeX，故采用 **Word 模板 + WPS** 路线。官方格式模板须在开赛后立即下载，未拿到前不写正文（页数、字号、摘要页、编号页规则以官方模板为准）。

---

## 3. 架构总览

```
数模作战包/
├── SOP-执行规范.md      ← 用户在 P0–P2 段全程翻的那份
├── 验收规范.md          ← 全队共用，V0–V3 判据 + KPI + 角色矩阵
├── 选题评估卡.md        ← 硬门槛过滤 + 三维评估 + 拥挤度五信号
├── prompts/             ← 30 张卡，按 6 组分文件
│   ├── 0-选题.md  1-题面解析.md  2-建模.md
│   ├── 3-求解与自证.md  4-论文.md  5-交接与支援.md
├── SETUP.md             ← 环境准备 + 三条环境坑
└── scaffold/
    ├── project/         ← 复制为每题工作区（含空的事实源文件）
    └── scripts/         ← 9 个脚本，全部退出码 0=通过
```

### 3.1 六份事实源文件（全流程的血管）

固定 schema：**一行一条目、ID 前缀写死、字段用 `|` 分隔、禁止用散文段落承载关键信息**。理由：接手方的 AI 会直接读这些文件；散文里的约束会被漏读，字段不会。

| 文件 | ID | 字段 | 所有权 |
|---|---|---|---|
| `SPEC.md` | `C##` | 题面原文引用 \| 解读 \| 硬/软 \| 单位 \| 建议验收断言 \| 所属小问 | **只读**（仅 R1/R3 全员会可改） |
| `assumptions.md` | `A##` | 假设内容 \| 理由 \| 影响面 \| 验证方式 \| 若为假结论如何变 | 仅全员会可改 |
| `numbers.md` | `N##` | 值 \| 单位 \| 来源脚本路径 \| 出现位置 \| 复算方式 | 任何班可追加，不可改值 |
| `decisions.md` | `D##` | 决策 \| 备选 \| 选择理由 \| 放弃代价 \| 时间 | 追加式 |
| `issues.md` | `I##` | 定位(小问/`C##`) \| 类别 \| 影响面 \| 当前假设 \| 建议验证 \| 优先级 \| **复现命令** \| owner | 追加式，owner 唯一 |
| `handoff.md` | — | 当前状态 \| **已排除路线及原因** \| 最可疑三处 \| 下一步三条 | 交接班写，接接班追加 |

**硬规则：论文、代码、图表里只引用编号，不允许把数值或假设抄第二遍。**

---

## 4. 阶段与时间盒

默认为 4 天 ≈ 88h 有效工时。若明早确认赛制更短，按比例压缩 P2 与 P4，**选题、交接、V3 三个时间盒不压缩**。

| 阶段 | 时间盒 | 主力 | 出口 |
|---|---|---|---|
| 今晚 赛前准备 | — | AI | 作战包过 §10 的自检 |
| P0 选题 | ≤ 3h | 人判，AI 出表 | 定题 + `D01` |
| P1 题面解析 | ≤ 5h（与 P0 重叠 1h） | AI 抽取，人核对 | **Gate R1**，`SPEC.md` 冻结为只读 |
| P2 初版全案冲刺 | D0.5 → D1.5，≈22h | 用户 + AI | **V0 交接验收** |
| P3 交接会 | D1.5，2h | 三班会同 | 交接通过，进入两班制后段 |
| P4 支援期 | D1.5 → D4 | 另两班 + AI | **R2**（逐小问）、**R3**（提交前） |

**论文不独立成段**：论文骨架从 P2.1 就开始写，不等结果。空章节过不了 V1，所以这条是硬约束而非建议。

### 4.1 预先写死的砍单线

| 触发 | 动作 |
|---|---|
| 任一小问连续 3h 零进展 | 强制换模型或降复杂度，登记 `D##` 记理由 |
| D1 22:00 问题 1 仍无结果 | 立即降级为"简化版 + 正文明确说明" |
| 剩余 < 12h | 停一切新求解，全力成文（残缺论文有分，完整代码没有） |
| 剩余 < 4h | 只做 V3 修补与提交演练 |
| 提交前 90min | **内容冻结硬闸**，只允许 V3-5 格式类修改 |

---

## 5. 三轮审查会（时间点）与验收判据（内容）

命名关系：`R1/R2/R3` 是审查会；`V0–V3` 是会上用的判据。R1 ≈ V1 + 题面勾验；R2 = V2（逐小问触发，非最后统一）；R3 = V3；V0 仅在 P3 交接那一刻用一次。

### R1 题面审查（P1 末，全员到场 60–90min）
AI 产出：约束清单 / 歧义清单 / 假设表 / 小问依赖图。
三人分工：**甲**查"漏了哪条约束"、**乙**查"题意读大了还是读小了"、**丙**查"哪个小问其实做不了"。
不通过的动作是**回炉重读题面**，不是"改一改"。理由：此时返工最便宜，之后返工代价最高。

### R2 证据审查（每小问出结果即触发，15–20min）
**人不看代码，只看证据包。**任一判据红 → 该结论在论文里必须降级标注"未验证"，**不允许"先写着回头补"**。

### R3 一致性与可提交性审查（提交前 ≥3h 启动，120min）
先机器扫一致性，再 AI 切评委视角出"最可能被质疑的 12 问"，逐条给出处置。90min 硬闸后只允许格式修补。

---

## 6. 验收规范

### V0 交接验收（D1.5，60min，判定权归接手人，交接班闭嘴）

| 编号 | 判据 | 方式 | 不通过动作 |
|---|---|---|---|
| V0-1 冷启动 | 开**全新 AI 会话**，仅喂四样输入（题面原文、`SPEC.md`、`issues.md`、目录树），要求产出"每问要算什么 / 现在到哪 / 下一步三条"。与交接班所写 `handoff.md` 命中率 **≥80%** | `coldstart.py` 打包 + 人判命中 | 回炉补文档。**命中率不足不是新会话笨，是移交包欠规格** |
| V0-2 复述 | 接手人只读移交包 20min，复述任一小问的"目标/模型/当前结果/最可疑三处"，命中该问硬约束 ≥80% | 人判，对照 `SPEC.md` 逐条勾 | 补文档，不许"他们自己看代码" |
| V0-3 一键复现 | 接手人**自行**执行 `run_all.py`：结果与移交值一致、无报错、种子固定 | 脚本 | 交接失败，交接班当晚补 |
| V0-4 在册抽验 | 从 `issues.md` 抽 3 条跑其复现命令，确认描述真实且准确 | 脚本 + 人 | 虚假/夸大登记记为漏报 |
| V0-5 反向发现 | 接手人 30min 自由审查，尝试找出 ≥1 条**未登记**问题 | 人 | 找到即当场补登记并复盘"为何未被发现" |

核心原则：**登记过的瑕疵是资产，未登记的瑕疵是事故。**

### V1 结构验收（`check_structure.py`，≤10min，每小问完成即跑）
非 0 退出即禁止进入下一环节。检查项：目录与命名合规 · 每小问交付物齐（`spec.md`/`src`/`results`/`verify.py`）· 六份事实源文件存在且非空 · **占位符扫描**（`TODO`/`待补`/`XXX`/空章节，0 容忍）· `numbers.md` 每条有来源脚本路径 · 图表公式编号连续且正文有引用。

### V2 证据验收（逐小问，六项全绿才算该问成立）

| 判据 | 阈值 |
|---|---|
| V2-1 独立复算 | `verify.py` 须走不同算法路径（静态检查：不得 import 求解模块）。确定性算法相对误差 ≤1e-6；启发式/随机 ≤1% 且 5 个 seed |
| V2-2 量纲与取值域 | 每个 `N##` 有单位字段且与 `SPEC.md` 单位约定一致；换算只经单一 `units.py` |
| V2-3 退化特例 | ≥1 个可解析退化情形（N=1 / 无约束 / 参数取退化值）退回已知解析值或题面示例值，误差 ≤1% |
| **V2-4 约束逐条勾验** | `SPEC.md` 中该问**每条硬约束对应一个可执行断言且全绿，覆盖率必须 100%**。这是防止 AI 悄悄放宽约束刷出漂亮结果的唯一屏障 |
| V2-5 边界极端输入 | 空/最大/并列/不可行情形不崩、不输出无意义最优 |
| V2-6 可复现 | 固定 seed、`env.lock` 记版本、同输入两次运行逐位一致 |

### V3 一致性与可提交性验收（全队 120min）

| 判据 | 方式 | 阈值 |
|---|---|---|
| V3-1 数字四处一致 | `check_numbers.py` 扫正文，所有数值须为 `N##` 引用 | 同值不同源 0 项；同源不同值 0 项 |
| V3-2 假设闭环 | 每条 `A##` 正文至少引用 1 次且带影响说明；正文模型设定均可溯源到 `A##` | 无来源设定 0 容忍 |
| V3-3 符号与文献 | 符号表覆盖全部数学符号；参考文献可解析 | 未登记符号 0 |
| V3-4 图表 | 单位/编号/正文引用齐，矢量或 ≥300dpi，灰度可辨 | 0 |
| V3-5 格式合规 | 页数、摘要页、编号页、附录打包清单、文件命名、PDF 体积 | 逐项勾官方模板 |
| V3-6 评委挑刺 | AI 出 12 问，每条要么正文有答要么进"模型评价与推广" | 未处置 ≤1 |
| V3-7 附件完备 | `issues.md` 全部 P0 关闭；结果文件按题面要求命名/格式存在 | P0 未关 0 |

### 5 个 KPI（验收规范必须可打分）

| KPI | 定义 | 目标 |
|---|---|---|
| 瑕疵在册率 | 已登记 ÷（已登记 + 验收中新发现） | ≥90% |
| 约束覆盖率 | 有可执行断言的硬约束 ÷ 全部硬约束 | 100% |
| 数字台账引用率 | 正文中带 `N##` 的数值 ÷ 全部数值 | ≥95% |
| 一键复现成功率 | 接手人自行跑通 ÷ 尝试 | 100% |
| 冷启动通过率 | 新会话命中 ≥80% 次数 ÷ 尝试 | 100%（交接前后各跑一次） |

### 角色矩阵
V0 接手人判 · V1 脚本（谁交谁跑）· V2 作者跑 + 第二人判 · V3 全队，丙主持 · 交叉复算的"验证方"由**非求解班组**担任。

---

## 7. 多 AI 并行的防腐机制

三台 AI 同时作业的技术收益是互不信任的三次验证，风险是事实分叉。四条机制：

1. **`SPEC.md` / `assumptions.md` 只读。**任何 AI 不得改题面约束的解读；要改只能提 `I##`，由 R1/R3 全员会裁决。
2. **按 `I##` 分区作业。**一个条目只有一个 owner；不分区必然冲突。
3. **交叉复算。**A 班 AI 求解的结果，必须由另一班 AI 写的 `verify.py` 独立复算。同一条 AI 路径自证为绿不算绿。
4. **`handoff.md` 必含"已排除路线及原因"。**三 AI 并行最大浪费是后一班把前一班的坑重踩一遍，因为它看不见失败历史。每条格式：`路线 | 试到什么程度 | 为何放弃 | 证据文件路径 | 什么条件下值得重试`。

**用户的 P4 角色固定为编排者 + 仲裁者**，只做三件事：分派 `I##`、跑 V1/V2 脚本、在两班 AI 结论冲突时按编号证据裁决。**不亲自下场写代码**——一写就把仲裁位让出去了。

---

## 8. 执行规范（我侧）

### 8.1 P0 选题（≤3h，内部严格排序）

| 步 | 时间 | 动作 | 为什么在这个位置 |
|---|---|---|---|
| P0.a | 0–5min | 拿到全部题面（PDF/DOCX） | — |
| P0.b | 5–25min | **硬门槛过滤 + 拥挤度推断**（只读题面，不需附件） | 拥挤度评估不依赖下载，把它排前面可避免把最贵的等待串行化 |
| P0.c | **并行**，5–60min | 另一名队员下载全部附件并跑 `附件数据体检卡` | 体检结论反过来修正拥挤度的 S3 信号 |
| P0.d | 25–85min | 三维评估（可达性/风险/拥挤度）→ 剩 2 候选 | — |
| P0.e | 85–145min | 两候选各跑可行性探针，每候选 30min，各出 1 页 memo | — |
| P0.f | 145–175min | 裁决，写 `D01`（含放弃另外各题的理由） | 明天复盘要用 |

**硬门槛（任一命中直接出局，不进评分）**：需要编译原理/计算机体系结构背景；需要信息论或通信 PHY 层推导；需要专有硬件或无法获得的数据。
理由：这类题也是 AI 最容易产出"看似完整、实则错误"结果的题型。

**三维评估**

- **① 二等奖可达性**：完成度可交付性（四问都能交东西吗）· 评分弹性（有无客观数字被认出来，纯主观题区分度差）· 创新展示位（有无一处让评委记住）· AI 杠杆率（调参/跑基线/画图杠杆高，物理建模正确性杠杆低）。
- **② 风险**：数据可得性（一票否决）· 算力与时间爆雷 · 结果不可自证（无客观判据则只能靠论证，写作成本最贵）· 评委主观性 · 同质化（同一公开数据集 + 同一结构，区分度变成运气）。
- **③ 拥挤度**（见 §8.2）。

### 8.2 拥挤度五信号（全部由题面观测，每张卡必须附"我凭什么这么判"并引用题面原文）
| 信号 | 量化方式 | 方向 |
|---|---|---|
| S1 开放性（**最强预测器**） | 题面是否给定评价指标/阈值/结果文件格式；"请建立体系评价 X"式表述 → 弱队也敢交卷 | 越开放越挤 |
| S2 技能栈宽度 | 所需技能是通用（回归/规划/评价）还是领域专有；代理指标：非通用专业术语密度（术语数/千字） | 越通用越挤 |
| S3 数据与格式摩擦 | 附件规模、格式常规度（xlsx/csv 零摩擦；DWG、雷达基数据、netCDF、二进制高摩擦）、是否需网盘/外网/GPU | 摩擦越高越冷 |
| S4 题材热度 | 命中年份导向关键词（低空经济、双碳、AI/大模型、智慧农业、交通、文旅美学…） | 越热越挤 |
| S5 算力与工作量天花板 | 数据量级、是否需训练、4 天内单机是否可完成 | 越重越冷（双刃，见下） |

加权：S1×1.5、S2×1.2、S3×1.2、S4×1.0、S5×0.8 → 归一化 `crowd ∈ [1,5]`。
映射为二等奖门槛线：`crowd ≥4` → 选做占比 >30%，需该题前 8%；`3–4` → 15–30%，前 12%；`2–3` → 8–15%，前 18%；`≤2` → <8%，前 25%。

**两条使用规则**
- 只用于排序与淘汰，不用于绝对概率。题面推断误差量级 ±10 个百分点，够判"A 比 B 挤"，不够判"我们能进前 8%"。
- **机会题例外**：`S5 ≥ 4` 且 `可达性 ≥ 3.5` 时，覆盖拥挤度扣分直接进候选。这类题冷是因为做不出来、不是没人想做，只要本队 AI 主力真跑得动，性价比最高。

### 8.3 决策规则（写死，防现场摇摆）

> 选"本队能力剖面与该题评分剖面错位最大"的题，而不是"最好做"的题。
> 具体：在 `可达性 ≥3.5` 且 `风险 ≤3` 的题里选 **拥挤度最低**者；三题并列时选**评分弹性最高**者（宁要能算出数字的题，不要靠审美判断的题）。

### 8.4 P1–P2 步骤

| 步 | 时间盒 | 动作 | 硬规则 |
|---|---|---|---|
| P1 | 3–5h | 题面解析四连卡 → R1 | 通过后 `SPEC.md` 转只读 |
| P2.1 | 3h | 每问 3 候选模型 + 下界粗估 + **论文骨架与编号先定** | **没有粗估区间不许写求解代码**；无来源设定进 `A##` 前不得进正文 |
| P2.2 | 15h | 按小问并行求解；每问出结果即跑 V1 + V2 + 缺陷登记 | 每小问 AI 自报可疑点 **≥3 条**，0 条视为未认真检查 |
| P2.3 | 5h | `handoff.md` + 数字回填 + 自跑一次冷启动测试 | 骨架必须含全部章节与编号，空章节过不了 V1 |

### 8.5 prompt 卡片库（30 张，6 组）

每张卡固定结构：**触发时机 / 输入 / prompt 正文骨架（含 `{占位变量}`）/ 必须落到的文件与格式 / 30 秒人工判定项 / 已知失效模式 / 验证状态**。

`验证状态` 取三值之一：`未压测` / `已压测@<夹具>` / `已修订@<日期>`。今晚 dry-run 只能压到组 0 与组 1 共 10 张，**其余 20 张明天第一次用时按"未验证工具"对待**：先在低风险小问上用一次，确认输出形态没问题，再全量用。这条写进 `SOP` 首页，不可省。

| 组 | 卡 |
|---|---|
| 0 选题（5） | 硬门槛过滤 · 拥挤度推断 · 三维评估（可达性/风险） · 可行性探针 · 定题裁决 |
| 1 题面解析（5） | **约束抽取**（强制 schema，宁滥勿缺，单列"我不确定怎么读的地方"） · 歧义分叉 · **漏读自查**（新会话只给题面，反问"上一版漏了哪 5 条"） · 小问依赖与保底 · 附件数据体检 |
| 2 建模（3） | 三候选模型（教科书法/结构改进/数据驱动，推荐 1 但必须写为何不选另 2） · 下界与粗估 · 单位坐标系与符号表约定 |
| 3 求解与自证（5） | 求解实现（附"三不"：不许放宽硬约束、不许硬编码答案、不许跳过失败分支） · 自证包（V2 六项） · 缺陷登记 · 交叉复算 · 调参日志 |
| 4 论文（6） | 章节树先定 · 摘要三段式（问题—方法—关键数字） · 数字回填（只填 `N##`） · 图表统一风格 · 评委挑刺 12 问 · 模型评价与推广 |
| 5 交接与支援（6） | handoff 生成（含已排除路线） · 冷启动测试 · `I##` 分派 · 冲突仲裁 · 每日站会快照 · 提交前硬闸检查 |

### 8.6 值守节奏
每 6h 一次站会（10min）：跑 `check_structure.py` + 比对时间盒偏差 + `issues.md` 的 P0 存量。站会快照由卡 5-5 生成，输出到 `log/`。

---

## 9. 脚手架

### 9.1 目录（`scaffold/project/`，复制即工作区）
```
SPEC.md assumptions.md numbers.md decisions.md issues.md handoff.md README.md
data/raw/  data/processed/  data/REPORT.md
q1/ q2/ q3/ q4/ …          每问：spec.md  src/  results/  verify.py
paper/sections/  paper/figures/  paper/tables/  paper/main.docx
tools/units.py  tools/common.py
log/    env.lock
```

### 9.2 脚本（9 个，退出码统一 0=通过、1=失败、2=脚本自身异常）

| 脚本 | 职责 |
|---|---|
| `bootstrap.py <题号> <小问数>` | 建工作区、生成六份事实源文件空壳（含 schema 表头与示例行） |
| `check_structure.py` | V1 全项，含占位符扫描与逐问交付物完备性 |
| `check_numbers.py` | V3-1：扫正文数值是否带 `N##`，比对 `numbers.md` 同源一致性 |
| `run_all.py` | V0-3 一键复现：按 `q*/run.sh` 顺序执行 + 结果比对 |
| `env_lock.py` | V2-6：Python 版本 + `pip freeze` + GPU/CUDA + seed 快照 |
| `coldstart.py` | V0-1：打包四样输入（题面/SPEC/issues/目录树）成单文件，便于喂新会话 |
| `plot_style.py` | 统一 matplotlib 风格：字号、线宽、中文、灰度可辨、单位入轴、输出 pdf+png 双份 |
| `numbers_add.py` | 追加 `N##` 条目（值/单位/来源脚本），防手改 |
| `issue_add.py` | 追加 `I##`（复现命令必填，否则拒绝） |

### 9.3 SETUP.md（今晚执行）
```
# 1. 解释器：PATH 上的 python 是 WindowsApps 空壳，必须用全路径
PY=/f/anaconda/envs/ptg/python.exe      # F:\anaconda\envs\ptg\python.exe

# 2. 补包（走国内镜像）
$PY -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple \
    numba pywt statsmodels shap lightgbm python-docx pulp deap ezdxf pdfplumber
$PY -m conda install -y -c conda-forge xarray netcdf4 geopandas

# 3. 已知环境坑（明天现场排查用）
#    a) 裸 python / pip 会命中 WindowsApps 空壳
#    b) 无 LaTeX/MATLAB/R：论文只能 Word/WPS，公式在 Word 内排
#    c) GitHub 不通：装包一律走 tuna 镜像；不要现场 pip install 未试过的源
```
`torch 2.9.0+cu128` 已可用，明天不再动 DL 环境（**环境一旦能跑就冻结**）。

---

## 10. 今晚实施顺序：写卡 → 压卡 → 修卡

**顺序硬约束**：30 张卡必须先写出 v1，dry-run 才有东西可用。dry-run 产出的是**修订**，不是卡片本身。故今晚分四段，总计约 3.6h，目标 23:30 前收工（明天是 36h 冲刺，体力算进预算）。

| 段 | 时长 | 内容 | 出口判据 |
|---|---|---|---|
| **T1 地基** | 45min | 环境补包（§9.3）· `scaffold/project/` 目录 · 六份事实源文件的空壳 schema · 9 个脚本中**先写全** `check_structure.py`/`check_numbers.py`/`bootstrap.py`，其余 6 个可先最小可用 | 三脚本跑通；两个 checker 对"故意留错"敏感、修好后转绿 |
| **T2 写卡** | 75min | AI 按 6 组分头写 30 张卡 v1 + `选题评估卡.md` + `SETUP.md`。**用户不逐张读**，只审 §8.5 里加粗的 4 张：`约束抽取`/`缺陷登记`/`漏读自查`/`冷启动测试` | 30 张卡齐、每张带 `验证状态=未压测`；4 张关键卡经你签字 |
| **T3 压卡** | 75min | 见下方压测表，产出 `dryrun-log.md` | ≥3 张卡被判"需修订" |
| **T4 修卡打包** | 20min | 修订回写、状态改 `已修订@2026-09-22` · 生成**《明早 15 分钟上手卡》**一页（开赛 0–60min 动作、前 36h 时间盒、五条砍单线、V0 判据、环境三条坑） | 作战包 v1.1 可上场 |

### T3 压测内容与夹具（只覆盖组 0 + 组 1，共 10 张卡）

| # | 时长 | 压什么 | 夹具 | 通过判据 |
|---|---|---|---|---|
| 3.1 | 30min | `硬门槛过滤` + `拥挤度推断` + `三维评估` | 2025 全六题 | 判定顺序为 **C/E/F 热题、D 数据摩擦型偏冷、A/B 被硬门槛淘汰**。顺序错则调 S1–S5 权重；**只校顺序，不校绝对值** |
| 3.2 | 20min | `约束抽取` + `缺陷登记` + `漏读自查` | **C 题**（坑最密） | 抽出约束覆盖：孔向环绕坐标周期性 · 像素/mm 各向异性 · 1mm≈2.6px 细线阈值 · JRC 对采样密度 N 敏感 · 裂隙与岩层界面判据 · 正文只准呈现指定 3 张图。漏一条即记该卡缺陷 |
| 3.3 | 15min | `附件数据体检` 真实链路 | **E 题**（源域 = CWRU，公开可下） | 下载 → `data/raw/` → `data/REPORT.md`，且抓到采样率 12k/48k 不统一、正常类仅 4 样本、跨域转速不一致 |
| 3.4 | 10min | `coldstart.py` + `handoff 生成` | C 题产物 | 开 2 个全新会话，命中率 ≥80% |

### 今晚成功判据
1. 两个 checker 对故意留错敏感、修好后转绿；
2. 30 张卡全部存在且带验证状态字段，其中 **10 张转 `已压测`**；
3. 冷启动命中 ≥80%（2 次）；
4. **≥3 张卡被修订** —— 一张没改说明卡写得不够具体，没经受住实压；
5. `dryrun-log.md` 记录全部缺陷与修订，作为 v1.1 的变更来源（这些缺陷本身就是"瑕疵必须在册"原则对作战包自身的适用）。

### 降级顺序（时间不够时）
先砍 3.2，再砍 3.1 的三维评估；**绝不砍 T2**。脚手架坏了明天没法上场，模型方案少了能临场补，**卡没写出来则整套流程空转**。

---

## 11. 已识别风险与缓解

| 风险 | 缓解 |
|---|---|
| 前 36h 冲刺被"求完美"拖走，D1.5 交不出东西 | 初版定义 = 完整骨架 + 瑕疵全登记，**不含**"结果正确"。V0 只验可交接性不验正确性 |
| 用户回到主力位写代码，仲裁位空缺 | §7 明写为禁令；站会快照卡第一项就是检查用户本时段是否产生了代码提交 |
| 附件当天下载受阻（网盘/官网限流） | P0.c 与选题并行下载，不留到 P1；SETUP 记录已试过的可用镜像 |
| AI 悄悄放宽硬约束刷出漂亮结果 | V2-4 约束逐条断言 + 100% 覆盖率要求，是唯一屏障 |
| 三台 AI 结论分叉 | `SPEC.md` 只读 + `I##` 分区 + 交叉复算 + 编号仲裁 |
| Word 无版本管理，多人改稿冲突 | `paper/` 按节分文件；改稿权按 `I##` 分配；导出 PDF 前必过 V3 |
| 明天赛制时长与本文 4 天假设不符 | 时间盒参数化（§4）：压缩 S 段，不压缩选题/交接/V3 三个盒 |
| 官方格式模板未拿到就写正文 | §2.3 规定拿到前只写 `paper/sections/` 纯文本，不排版 |

---

## 12. 参数与默认值（开赛后按实际情况覆盖，均为非阻塞）

| 参数 | 默认 | 来源 |
|---|---|---|
| 赛制总时长 | 4 天（09-23 开赛） | 官方通知，明早核对 |
| 论文页数上限 | 不限页 + 摘要 1 页 | 官方《论文格式》模板 |
| 排版工具 | Word / WPS | 本机无 LaTeX |
| 提交物 | 论文 PDF + 源程序 + 结果文件 | 题面与官方规范 |
| 附件下载渠道 | 官网 + 网盘（现场探测） | 题面 |
| 求解语言 | Python（`ptg` 环境）为主，必要时 C++ 补关键路径 | 本机环境 |

---

## 13. 自审结论（2026-09-22）

**扫描项**：占位符、内部一致性、范围、歧义。

**发现并已在本文内修正的六处问题**
1. P0 内部步骤加和为 175min，与阶段表的"≤2.5h"矛盾 → 阶段表与 §8.1 统一为 **≤3h**。
2. 阶段表遗留旧命名 `S0`、正文遗留 `S6/论文不独立成段` → 全文改为 P 段命名，今晚段不带编号。
3. §8.2/§8.3 标题层级错挂为 §8.1 的子节 → 提为同级 `###`。
4. 压缩规则表述"按比例压缩 S 段"指向已不存在的编号 → 改为"压缩 P2 与 P4，选题/交接/V3 不压缩"。
5. **"四件套"一名两义**（既指六份事实源文件，又指每小问的 `spec/src/results/verify`），且标题写"四"而表列为六 → 事实源改称**六份事实源文件**，逐问改称**每小问交付物**，全文消除词形冲突。
6. **（由用户发现，我的自审漏了）§10 通篇在"跑某某卡"，却从未安排"写卡"这一步** —— 等于假设 30 张卡凭空存在，且旧成功判据"30 张卡每张至少用过一次"在今晚物理上不可达。已改为 **T1–T4 四段（含 T2 写卡 75min）**，给卡片新增 `验证状态` 字段，并显式声明**今晚只能压 10/30 张**、其余 20 张明天按未验证工具对待。

**通过项**
- 卡数：§1、§3、§8.5 三处均记 30 张，分组加和 5+5+3+5+6+6=30 核对一致。
- 脚本数：§3 目录树记 9 个，§9.2 表列 9 行一致。
- 今晚 dry-run 时长：§10 六步加和 180min，与"3h"表述一致。
- 编号引用：`卡 5-5` 对应 §8.5 组 5 第 5 张（每日站会快照），核对成立。
- 歧义收敛：初版"完成"的定义已固定为"完整骨架 + 瑕疵全登记 + 可一键复现"，**不含结果正确**；V0（验可交接性）与 V2（验正确性）边界已分开。
- 范围：单一交付物（作战包），一次实施可完成；题目求解明确列为非目标。
- §12 六个外部参数全部给了默认值，无阻塞项。
