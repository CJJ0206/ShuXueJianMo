# 组 3 求解与自证（卡 3-1 … 3-5）

本套验收的技术核心，明天最可能出错的地方全在这里。卡片与具体赛题无关，题目相关内容以 `{花括号变量}` 注入。
今晚 Task 16 只用高教社杯 B 题最小闭式计算压 卡 3-1 / 3-2 / 3-4 三张，压过后把这三张的`验证状态`改为
`已压测@高教社杯B题最小计算`（现在一律保持 `未压测`）；未压到的两张明天首次按**未验证工具**对待：
先在低风险小问上用一次，确认输出形态，再全量用（spec §8.5）。

**三条贯穿本组的机制**（spec §6 V2、§7 防腐四条）：
1. **同一条 AI 路径自证为绿不算绿**——求解（卡 3-1）与验证（卡 3-2 的 `verify.py`）不得复用同一算法族；
   跨班复算由卡 3-4 承担，且验证方**不得读求解源码**。
2. **V2-4 是唯一防止 AI 悄悄放宽约束刷结果的屏障**——覆盖率必须 100%，且由脚本判定不由人叙述。
3. **登记过的瑕疵是资产，未登记的瑕疵是事故**——卡 3-3 强制自报 ≥3 条。

条目行格式契约见 `数模作战包/scaffold/scripts/schema.py`（本组卡片要求输出的字段顺序与它逐字一致）：
`issues.md` → `定位|类别|影响面|当前假设|建议验证|优先级|复现命令|owner|状态`（ID 前缀 `I`）；
`numbers.md` → `值|单位|来源脚本|出现位置|复算方式`（前缀 `N`）；`assumptions.md` → `内容|理由|影响面|验证方式|若为假后果`（前缀 `A`）；
`decisions.md` → `决策|备选|理由|放弃代价|时间`（前缀 `D`）；`SPEC.md` → `原文引用|解读|软硬|单位|验收断言|所属小问`（前缀 `C`，**只读**）。
工作区结构由
`/f/anaconda/envs/ptg/python.exe 数模作战包/scaffold/scripts/bootstrap.py <工作区根> <小问数>` 生成
（`q*/spec.md`、`q*/src/`、`q*/results/`、`q*/verify.py`、`tools/units.py`、`log/`）。
`run_all.py <工作区根>` 依次以 **cwd = `q*/`** 执行 `q*/src/main.py` 与 `q*/verify.py`，超时 1800s，非 0 即 FAIL。

---

### 卡 3-1 求解实现
- **触发时机**：P2.2 每问一次（换模型路线时对该问再跑一次）；**前置条件是卡 2-2 的粗估表已落进 `q*/spec.md`**
- **输入**：`{小问号}`、`{卡 2-1 选定模型与其目标函数式}`、`{SPEC.md 该问全部 C## 行（H 类逐条）}`、`{卡 2-2 粗估表}`、`{tools/units.py}`、`{附件路径与列名}`
- **prompt**：
```text
你是实现工程师。把选定模型写成可被一键复现的求解脚本。禁止改模型、禁止放宽约束。

【三不】违反任一条即为本次交付不合格，必须重写而不是解释：
1. 不许放宽 SPEC.md 里任何 H 类约束来让结果可行或变好看。
   SPEC.md 只读。确实需要放宽时：停下来，只输出"需要放宽 Cxx，因为 ……"，
   并给出放宽后的下界影响；不许先改了再说。惩罚项权重等价于放宽约束，同样禁止悄悄做。
2. 不许硬编码答案，不许为了凑指标修改阈值、容差、粗估区间或验收断言。
   所有阈值只能来自 SPEC.md/卡 2-2/tools/units.py，并在代码注释里写来源编号。
3. 不许用 `except: pass`、`except Exception: pass`、`contextlib.suppress` 或
   任何"跳过失败样本/失败迭代"的写法让循环看起来跑完。
   失败必须显式：raise（附上下文）或返回 status="infeasible"/"failed" 并计入 result.json 的失败清单。
   不可行解不是数值误差：必须被标出来，禁止当作最优解输出。

【四条交付要求】
A. 随机性全部固定 `seed=42`：random / numpy / torch(cpu+cuda) / sklearn 的 random_state 逐个设，
   封装成一个 `set_seed(42)` 函数在 main() 第一行调用；文件头注释写 `SEED = 42`。
B. 写 `q{小问号}/src/main.py`，必须可被 run_all.py 调用：
   - 无必填命令行参数、不读标准输入、不弹交互；`def main() -> int` + `if __name__ == "__main__": sys.exit(main())`；
   - 成功返回 0，失败返回非 0（不要用异常逃逸造成的 1 来表示"正常"）；
   - **run_all.py 的 cwd 是 `q{小问号}/`**，所以路径一律用
     `ROOT = Path(__file__).resolve().parents[2]` 再拼，禁止裸相对路径 `"data/…"`；
   - 一切单位换算 `from tools.units import …`，src 里不得出现裸换算系数；
   - 顶部注明算法路径：`# METHOD: <算法族名，例：分支定界 / 整数规划 / 网格搜索>`（卡 3-4 要靠它判是否同族）。
C. 结果写 `q{小问号}/results/result.json`，UTF-8、`ensure_ascii=False`，至少含这些键：
   {"question", "method", "value", "unit", "timestamp", "seed", "params",
    "spec_c_ids_checked", "feasible", "runtime_s", "peak_mem_mb", "failed_items"}
   - `unit` 与 `timestamp`（`time.strftime("%F %T")`）不得为空；
   - `spec_c_ids_checked` 列出本结果实际满足并被检查的 `C##`；
   - `params` 必须是"本次真实使用的参数组"，与卡 3-5 台账的某一行一一对应。
D. 结束时打印**一行**运行时长与内存，例：
   `[runtime] wall=123.4s peak_mem_MB=512.3 (tracemalloc；psutil RSS=…)`
   用标准库 `tracemalloc.get_traced_memory()[1]/1e6`，需要 RSS 时再 `import psutil`。

【还必须同时输出未调参基线】
E. 第一次运行必须用**默认参数、未调参**的配置，其结果单独写
   `q{小问号}/results/result_baseline.json`（键同 C），并把这一行记进 `log/tuning.md` 的第 1 行。
   只交调过参的最优结果 = 宣称收敛，判不合格。

【边界与自证友好性】
F. 在 main() 里就处理并返回：空输入、最大规模、并列最优、不可行四种情形（卡 3-2 的 V2-5 要读它们），
   不许在四种情形下输出"看起来像答案"的数（例：不可行时 value 写 null、feasible 写 false）。
G. 数值结果不要 print 到 stdout 之外的任何地方；禁止写工作区以外的文件。
H. 末尾用一行声明与卡 2-2 粗估区间的对照：`GOT=<值> EXPECT_RANGE=<区间> IN/OUT`；
   落在区间外时，按粗估表第 4 列的四步解释顺序逐条回答，不许跳步，不许改区间。
```
- **必落文件**：`q{小问号}/src/main.py`、`q{小问号}/results/result.json`、`q{小问号}/results/result_baseline.json`；采用的那个数值经 `/f/anaconda/envs/ptg/python.exe 数模作战包/scaffold/scripts/numbers_add.py <工作区根> <值> <单位> q{小问号}/src/main.py <出现位置> <复算方式>` 追加进 `numbers.md`（**五字段顺序即 schema 顺序，不许手改**）；基线行进 `log/tuning.md`（卡 3-5）；"需要放宽 Cxx"这类拒绝写 `issues.md`
- **30 秒人工判定**：跑 `/f/anaconda/envs/ptg/python.exe q{小问号}/src/main.py` 两次（cwd 切到 `q{小问号}/`，与 `run_all.py` 一致），第二次除 `timestamp/runtime_s/peak_mem_mb` 外应逐位一致；`grep -nE "except|suppress" q{小问号}/src/main.py` 逐条看是否有 `pass` 吞异常；`result.json` 里 `unit`、`timestamp`、`spec_c_ids_checked` 三键非空；最后一行 GOT/EXPECT_RANGE 的 IN/OUT 与粗估表对得上
- **已知失效模式**：① 把不可行解当"数值误差"放行，输出一个像答案的数 ② 调参调到能过就宣称收敛，且不交 `result_baseline.json`（明天另一班按默认参数复算必然不同）③ 用裸相对路径读附件，`run_all.py` 换 cwd 后直接崩，V0-3 一键复现挂 ④ src 里写死换算系数绕过 `units.py`，V2-2 挂 ⑤ 把 H 约束悄悄改成惩罚项（形式上没删约束，实际可违反）⑥ `except: pass` 跳过脏样本，样本选择偏差无人知晓 ⑦ seed 只设了 numpy 没设 torch/sklearn，V2-6 两次运行不一致 ⑤ （今晚实测）**带界搜索的最优点落在区间端点上**——此时「在界内找到最优」毫无意义。任何频率带/参数域/超参网格都须检查 argmin 是否撞边界，撞界即判无效并扩界重算
- **验证状态**：已压测@B题最小计算

---

### 卡 3-2 自证包
- **触发时机**：P2.2 每问出结果后**立刻**（触发 R2 证据审查，15–20min）；每问一次，改算法后重跑
- **输入**：`{小问号}`、`{q*/results/result.json}`、`{SPEC.md 该问 H 类条目及其"验收断言"列原文}`、`{卡 2-2 粗估表}`、`{卡 2-1 该候选的"可解析退化的特例"}`、`{env.lock}`
- **prompt**：
```text
你是证据包作者。把 V2 六条逐条变成**文件 + 断言代码**，禁止任何"已验证/已确认/符合预期"式叙述。
一条判据没有对应文件与断言，就是没有做。全部写进 `q{小问号}/verify.py`（六函数）并由 `main()` 汇总。

文件头部（前 15 行内）必须写四行元信息：
  # V2-1 算法路径：<本文件用的算法族名>；求解路径：<result.json 的 method>；两者不同族，差异是 <一句话>
  # 本算法自身的分辨率/收敛阈：<数值 + 含义，例：频率分辨单元 res=1/(N·Δν)=2.5e-4 cycle/cm；迭代收敛阈 1e-8>
  # TOL 由上一行的分辨率导出（不是手调来让两路对上的），闭式确定性算法才用 1e-6
  # 本文件不 import q{小问号}/src 下任何模块

【V2-1 独立复算】函数 `v2_1()`：EXPECT 用**另一条算法路径**重算（暴力枚举/闭式解析式/另一套库/对偶问题任选），
  GOT 从 `results/result.json` 读。静态自查必须是**真代码**、放在 `v2_1()` 第一行，失败即整包 FAIL（不许注释掉）：
    import ast, pathlib
    tree = ast.parse(pathlib.Path(__file__).read_text(encoding="utf-8"))
    mods = set()
    for n in ast.walk(tree):
        if isinstance(n, ast.Import):
            mods |= {a.name.split(".")[0] for a in n.names}
        elif isinstance(n, ast.ImportFrom) and n.module:
            mods.add(n.module.split(".")[0])
    assert "src" not in mods, f"verify.py import 了求解包 src：{sorted(mods)}"
    for n in ast.walk(tree):
        if isinstance(n, ast.Call) and getattr(n.func, "id", "") in {"exec", "eval", "__import__"}:
            raise AssertionError("动态加载求解代码 = 同一条 AI 路径自证，不算绿")
  （用 AST 而不是字符串匹配，因为字符串版会被自查语句自身误伤。AST 版已在 ptg 环境实测：
    无 src import 时通过；文件里植入一行 `import src` 后必红。禁止退化成"打印一下 mods 就完事"。）
  容差（按 V2-1 原文）：确定性算法相对误差 ≤1e-6；启发式/随机算法 ≤1% 且跑 5 个 seed 并报极差：
    rel = abs(GOT-EXPECT)/max(1.0, abs(EXPECT)); assert rel <= TOL
    启发式：seeds=[42,1,2,3,4]，输出 5 个值 + max-min 极差 + 均值，assert 极差/|均值| <= 0.01
  **补充（今晚 Task 16 会据实测修订本条）**：离散谱/迭代/启发式算法的容差须按该算法自身的分辨率或收敛阈定义，
  并在 verify.py 头部写明该分辨率数值；闭式确定性算法才用 1e-6。
  证据文件：`results/v2_1.json`{GOT, EXPECT, rel_or_bin_gap, TOL, tol_source, path_A, path_B, seeds?}
  反向检查：两路相对偏差 < 1e-12 而其中一路是离散谱/迭代法 → 判为"抄了同一条路"，写 `I##` 并不给 PASS。

【V2-2 量纲与取值域】函数 `v2_2()`：解析 `numbers.md` 该问每条 `N##`，断言
  ①"单位"字段非空（无量纲必须写 `-`）②与 `SPEC.md` 对应 `C##` 的单位列字面一致
  ③取值在 `log/symbols.md` 声明的取值域内 ④ 裸换算系数审计：扫描 `q{小问号}/src/*.py` 与本文件，
  把形如 `* 3.6`、`/ 60`、`* 1000`、`np.pi/180`、`* 1e-2` 这类**未走 tools/units.py 命名常量**的乘除命中行
  原样列进证据文件的 `bare_coeff_hits`（文件名+行号+该行）。判据：`assert bare_coeff_hits == []`；
  该扫描正则今晚实测：`* 3.6` / `/ 60` / `* 1000` / `np.pi / 180` 均命中，
  `* MM_PER_PX` 这类 units.py 命名常量不误报。
  确实合法的（例如题面给的角度 180°）不许删扫描、也不许放宽正则，而是逐条改写成 units.py 里的命名常量后复跑。
  证据文件：`results/v2_2.json`{每条 N##: {unit, spec_unit, domain, pass}, bare_coeff_hits}

【V2-3 退化特例】函数 `v2_3()`：≥1 个**可解析**退化情形（N=1 / 无约束 / 参数取退化值），
  喂给**同一条求解代码路径**，断言退回已知解析值或题面示例值，误差 ≤1%：
  EXPECT_ANALYTIC 的来源必须写清（闭式推导一行 或 C## 示例值），不许用真实数据的另一子集冒充解析值。
  证据文件：`results/v2_3.json`{case, EXPECT_ANALYTIC, source, GOT, rel, pass}
  说明行必须写："退化特例是独立于题面数据的正确性锚点；本条 FAIL 时 V2-1 的双路比对结论一律作废。"

【V2-4 约束逐条勾验】函数 `v2_4()`：**本条是唯一防止悄悄放宽约束的屏障，覆盖率必须 100%。**
  从 `SPEC.md` 解析出该问全部 H 类 `C##`，对**每一条**执行其"验收断言"列（不可执行的回去让卡 1-1 重写，
  不许在这里糊一句 assert True），写 `results/constraints.json`：
  {"C01": {assertion, got, expect, pass}, ...}
  然后 assert set(constraints.keys()) == set(该问 H 类 ID 集合) —— 键数不等即覆盖率不足，直接 FAIL；
  assert all(v["pass"] for v in constraints.values())
  禁止用 try 把某条断言的失败吞成 pass；禁止把断言写成"应满足孔径约束"这种不可执行的话。

【V2-5 边界极端输入】函数 `v2_5()`：跑 空输入 / 最大规模 / 存在并列 / 不可行 四种，
  断言不崩（返回码为 0 或受控非 0 且异常信息明确）、且不输出无意义最优：
  assert edge["infeasible"]["status"] != "optimal"
  v = edge["infeasible"]["value"]; assert v is None or v != v   # 后者是 NaN 的正确判法（v in (None, nan) 永假）
  证据文件：`results/edge.json`{empty, max, tie, infeasible} 各含 {input_desc, status, value, runtime_s}

【V2-6 可复现】函数 `v2_6()`：以同一输入连跑两次 `src/main.py`，
  把两次 result.json 除 {timestamp, runtime_s, peak_mem_mb} 外逐位比较，diff 写 `results/repro.diff`（必须为空）；
  并断言 `env.lock` 存在（`/f/anaconda/envs/ptg/python.exe 数模作战包/scaffold/scripts/env_lock.py <工作区根>` 生成）
  且其中 seed 约定行为 `seed=42`。

汇总：`main()` 逐条打印一行 `判据|PASS/FAIL|证据文件路径`，全 PASS 才 print("V2 PASS") 并 exit 0；
任一 FAIL 即 exit 1（R2 会上该问结论在论文里降级为"未验证"，不许"先写着回头补"）。
全部输出同时落 `results/verify_log.txt`（六行，同一格式）。
```
- **必落文件**：`q{小问号}/verify.py`（头部四行元信息 + 六函数 + 汇总 `main()`）、`q{小问号}/results/v2_1.json`、`v2_2.json`、`v2_3.json`、`constraints.json`、`edge.json`、`repro.diff`、`verify_log.txt`；R2 会上人判结论追加 `log/R2-问{小问号}.md`（六行判据 + 谁判的 + 降级了哪条结论）
- **30 秒人工判定**：只看 `results/verify_log.txt`——六行是否全 PASS 且每行带证据路径；再打开 `verify.py` 前 15 行，检查有没有写算法路径、分辨率数值、以及有没有 `import src`；最后数 `constraints.json` 的键数是否等于 `SPEC.md` 该问 H 类行数（人肉再核一次，脚本自证不算）
- **已知失效模式**：① `verify.py` 只是把求解代码换行重抄（"另一库同一算法"也算同族），交叉复算形同虚设 ② V2-4 只挑最容易的约束写断言，键数靠凑 ③ 拿 1e-6 去卡离散谱/迭代法，两路永远对不上，于是有人直接把 `TOL` 手调大——这是本卡最阴险的失效，必须要求 `tol_source` 写清分辨率来源数值 ④ 退化特例用真实数据另一子集冒充"解析值" ⑤ 用 `try/except` 把 FAIL 包成 PASS ⑥ 只 print 不落文件，交接方无法机器复核（V0-3/V0-4 挂） ⑤ （今晚实测）**双路一致是必要条件，不是充分条件**：两条实现共享同一错误假设时会漂亮地互相印证——实测两个都错的答案相差 1.00 bin 却判 PASS。V2-1 通过须四条同时成立：①双路一致 ②落在求解前独立写下的粗估区间内 ③合成锚点取回已知值 ④两路不得共享被检验的那个假设（排除同一干扰须各用一种机理）
- **验证状态**：已压测@B题最小计算

---

### 卡 3-3 缺陷登记
- **触发时机**：P2.2 每问 `verify.py` 跑完后、R2 会前，每问一次；此后每 6h 站会增量一次（卡 5-5）；**0 条视为未认真检查，必须重跑本卡**
- **输入**：`{小问号}`、`{q*/results/result.json}`、`{q*/results/verify_log.txt}`、`{SPEC.md 该问 H 类}`、`{assumptions.md}`、`{卡 2-2 粗估表}`
- **prompt**：
```text
你是找茬的验收方，不是表功的实现方。目标：把这条结果最可能被推翻的地方**全部登记在册**。

强制：本问至少输出 3 条可疑点。**0 条 = 未认真检查**。
若你认为确无可疑点，请改为写下"我认为本结果无任何可攻击面"并给出论证——这在数学建模里几乎不可能成立。

每条按 issues.md 九字段输出，字段顺序严格如下（ID 前缀 I，两位数起，由 issue_add.py 生成，不要手编 ID）：
定位|类别|影响面|当前假设|建议验证|优先级|复现命令|owner|状态
  定位：`q{小问号}/Cxx` 或 `q{小问号}/Nxx` 形式，指到具体条目；
  类别：从 {单位换算, 取值域越界, 约束被软化, 粗估失真, 交叉复算差异, 理解偏差, 数据异常,
        算法收敛, 边界情形, 可复现性, 交付物格式} 里选，不许自造新类别（要自造先问人）；
  影响面：**必须写明"推翻哪条结论"**，格式 `若为真则 N##/C## 的结论被推翻，偏 <方向><量级>`；
  当前假设：现在按什么在做（引 A## 或写"未登记的隐含假设：……"）；
  建议验证：一句可执行动作，指明跑哪个脚本或加哪条断言；
  优先级：P0（结论可能翻盘）/P1（要补证据）/P2（可解释但影响小）——至少 1 条 P0 或说明为何没有；
  复现命令：**能直接粘贴运行**，含解释器全路径与 cwd，例
    cd <工作区根>/q{小问号} && /f/anaconda/envs/ptg/python.exe -c "import json;d=json.load(open('results/result.json',encoding='utf-8'));print(d['value'],d['unit'])"
    禁止"打开代码看一下"这类无法机器执行的写法；
  owner：一个条目只有一个 owner（{本轮负责人}）；状态：由脚本自动填 open，不许改别人的条目。

攻击面清单（往这些地方找，逐条排查并说明"查到/没查到"）：
单位与坐标系（px↔mm、角度制、dBm、% ↔浓度）· 边界与并列 · 缺失值/异常值处理是否引入样本选择偏差 ·
H 约束是否被软化为惩罚项 · 收敛判据与迭代上限 · 粗估区间是否被事后改动 · 数据子集是否被挑过 ·
退化特例是否真的解析 · 时间盒是否导致提前截断 · 交付物命名与列序是否符合题面。

禁止：写"可以进一步改进""样本量可扩大""精度可以更高"这类无关痛痒条目——**这类不计入 3 条**，
每条都必须能指出"哪条结论会被推翻"。也不许把已经知道的实现 bug 降级成"待优化"。
```
- **必落文件**：`issues.md`（≥3 行 `I##|…`，一律通过 `/f/anaconda/envs/ptg/python.exe 数模作战包/scaffold/scripts/issue_add.py <工作区根> <定位> <类别> <影响面> <当前假设> <建议验证> <优先级> <复现命令> <owner>` 追加——**复现命令或 owner 为空脚本会直接拒绝**，状态列自动写 open，ID 自动续号）；"未登记的隐含假设"同时补 `assumptions.md` 一行
- **30 秒人工判定**：随机挑 1 条，把它的复现命令原样粘贴执行——跑不通即本卡判未通过；扫"影响面"列是否每条都出现 `N##`/`C##`（没有就是无关痛痒）；数条数 ≥3 且至少 1 条 P0。（今晚实测）**同一份复现命令出现在两条以上 issue 上即可疑**——脚本只挡得住空值，挡不住为填字段而糊弄；须逐条核实该命令真能跑出对应那条缺陷
- **已知失效模式**：① 写三条无关痛痒的"可以进一步改进"，不推翻任何结论（最典型假通过）② 复现命令依赖交互或写死本机绝对路径，明天另一台机跑不了（V0-4 抽验判为虚假登记）③ 漏 owner / 复现命令为空 → `issue_add.py` 拒绝、`check_structure.py` 直接 V1 FAIL ④ 把已知的实现缺陷写成"待优化"，V0-5 反向发现时被翻出来，记为漏报（事故）⑤ 三个 owner 同时改同一条（违反 §7 分区作业）
- **验证状态**：已压测@2025高教社杯D题

---

### 卡 3-4 交叉复算
- **触发时机**：P2.2 每问，卡 3-2 之后由**非求解班组**执行（spec §7 第 3 条 + §6 角色矩阵"交叉复算的验证方由非求解班组担任"）；R3 前完成
- **输入**：只允许以下五样 —— `{SPEC.md}`、`{assumptions.md}`、`{numbers.md}`、`{q*/spec.md 该问题面}`、`{q*/results/result.json}`
- **prompt**：
```text
（本卡由**另一班组**的 AI 执行，与求解方不共享上下文。）

你是外部复核方。你要在**不看求解方任何代码**的前提下独立算出同一问的答案。

硬边界（违反即本次复算作废，重跑）：
  - **不要把 q{小问号}/src/ 目录放进上下文**；不要 Read/cat/grep 该目录下任何文件；
    不要引用其中出现的函数名、变量名、中间量名，也不要"猜它是怎么实现的"。
  - 可输入只有：SPEC.md、assumptions.md、numbers.md、q{小问号}/spec.md、q{小问号}/results/result.json（只读数值）。
  - result.json 的 `method` 字段只用来确认"你要走不同族路径"，禁止参考其 params 或预处理细节。
  - 禁止修改对方 result.json 与任何 results/ 下已有文件。

步骤：
1. 先声明你的算法路径（一行）：`MY_PATH=<算法族名>；与求解方 method=<对方族名> 不同族，理由是 <一句话>`。
   若你想不出第二条不同族路径，就停下来报告"本问无法独立复算"并写 `I##`（类别=交叉复算差异），不许偷偷抄。
2. 写 `q{小问号}/verify_b.py`，头部三行：
   # MY_PATH: <你的算法族>
   # 本文件未读取 q{小问号}/src/ 下任何文件（若读过请现在终止并报告）
   # TOL: <按卡 3-2 的定义：闭式确定性 ≤1e-6；离散谱/迭代/启发式按本算法自身分辨率或收敛阈，并写明该数值>
3. 输出差异报告 `q{小问号}/results/crosscheck.md`，每个被复算的量一行，四字段严格如下：
GOT|EXPECT|相对偏差|是否满足容差
   GOT=对方 result.json 的值（含单位），EXPECT=你独立算出的值（含单位），
   相对偏差写成数值 + 判据形式（例 `2.3e-8 ≤1e-6`；离散谱写成 `0.28 个分辨单元 ≤1.0`），
   "是否满足容差"只写 YES/NO，并另起一行写 NO 时你怀疑的**原因**。
   禁止只给一个布尔值不给数值与原因。
4. 两路不一致时：**禁止判谁对，禁止改对方阈值或容差**。只允许输出一节
   【两种实现各自的假设差异清单】，每条一行，格式：
   `差异点|求解方隐含假设（引 A## 或写"未登记"）|本方假设|该差异会让 GOT 偏 <方向><量级>|需要全员会裁决？是/否`
   未登记的隐含假设一律另用 issue_add.py 开 `I##`（类别=交叉复算差异），由卡 5-4 裁决。
5. 若两路**吻合到远超该算法分辨率的程度**（例：离散谱/迭代法相对偏差 <1e-12），第一反应必须是
   "我或对方违规共享了实现"，写 `I##` 并要求双方重跑，不要当好消息。
```
- **必落文件**：`q{小问号}/verify_b.py`、`q{小问号}/results/crosscheck.md`（四字段差异表 + 不一致时的假设差异清单）、不一致产生的 `issues.md` 行（九字段，经 `issue_add.py`）；**本卡不写 `decisions.md`**（裁决归卡 5-4）
- **30 秒人工判定**：跑
  `grep -nE "^[[:space:]]*(from|import)[^#]*src|open\([^)]*src|importlib|exec\(|eval\(" q{小问号}/verify_b.py`
  **应为 0 命中**（今晚实测：`from q1.src import main` 与 `open("q1/src/main.py")` 各命中一次，
  而头部声明句"本文件未读取 q1/src/…"不误报；所以别用裸 `grep src`，那条会把自己逼到删注释）。
  再抽对方 `src/main.py` 里的 3 个标识符 grep 本文件，出现即为污染；`crosscheck.md` 每行有数值与判据（不是纯 YES/NO）
- **已知失效模式**：① **读了 src 之后照抄，于是两路必然一致**——本卡最危险的失效，它会让"三次互不信任的验证"退化成一次自证；判据：离散算法给出 1e-12 级一致 / 双方变量名同名 / 两路代码结构同构 ② 差异报告只给布尔值，无 GOT/EXPECT 数值，交接方无从复核 ③ 把自己班实现当标准答案，把对方合理差异写成"对方错"（越权裁决）④ 顺手把容差改大以达成一致（等于共同放宽 V2-1）⑤ 用对方 params 里的调参结果当自己的默认配置 ③ （今晚实测）两路「独立」却共享同一个错误前提（例：都假定采样等间隔）。独立性的定义必须是「对被检验的那个假设各持一种立场」，否则交叉复算只是自我印证
- **验证状态**：已压测@B题最小计算

---

### 卡 3-5 调参日志
- **触发时机**：P2.2 每次跑参数实验即追加一行（**失败的也要记**）；R2 会前汇总一次，交接前再汇总一次
- **输入**：`{小问号}`、`{选定模型的参数网格}`、`{该问 H 类约束清单}`、`{q*/src/main.py 与 result.json 的 params 键}`
- **prompt**：
```text
你是实验台账管理员。规则：凡未进台账的运行，等于没有发生，也不许进论文。

在 `log/tuning.md` 里维护一张表，表头一行 + 每次运行一行，六字段严格如下：
参数组|seed|目标值|约束是否全过|耗时|结论
  参数组：写成可复制回 `main.py` 的 dict 字面量，例 `{"k":5,"tau":0.3,"max_iter":2000}`；
  seed：整数；纯确定性算法写 `-` 并在"结论"里注明"确定性，无 seed"；
  目标值：数值 + 单位（与 tools/units.py 一致），不可行/失败写 `infeasible` 或异常摘要，不许留空；
  约束是否全过：只允许两种写法 —— `全过` 或 `放宽了 Cxx`（后者必须点名是哪条 H 约束，
    并同步用 issue_add.py 开一条 `I##`（类别=约束被软化）；不许写"基本满足"）；
  耗时：秒，一位小数（与 result.json 的 runtime_s 同源，不许手填好看的数）；
  结论：只允许 `采用` / `弃用` / `待定` + 一句可检验的原因（例"弃用：tau=0.5 时 V2-3 退化特例误差 3.1%>1%"）。

强制：
1. **第 1 行必须是未调参基线**：`参数组` 列原样写 `DEFAULT（未调参）`，seed 写 42，
   目标值取 `results/result_baseline.json` 的值。这一行永不删除，即使它最差。
2. 论文正文只允许引用本表已台账化的行。任何出现在论文/摘要/图注里的参数取值，
   必须能在本表找到对应行；找不到就不要写进论文，也不要事后补一行"当时忘了记"。
3. 被标 `采用` 的唯一一行，其目标值由 numbers_add.py 入账 `numbers.md`
   （五字段顺序：值|单位|来源脚本|出现位置|复算方式；"复算方式"写 `见 log/tuning.md 第 N 行 + q{小问号}/verify.py`）。
4. 禁止把本表当"敏感性分析"往论文里搬：敏感性分析是**另一次系统扫描**（固定其余参数、逐参数 ±10%、有断言输出），
   由卡 4-x 单独做并落 `results/sensitivity.json`；调参台账不是它。
5. 末尾给一行汇总：`共 M 次运行 / 采用第 N 行 / 因约束不满足弃用 K 行 / 未调参基线目标值 = X`。
   只有 1–2 行的表说明漏记，请回去把跑过的都补上（补记的行在"结论"列标 `补记`）。
```
- **必落文件**：`log/tuning.md`（表头 + 基线行 + 每次运行一行 + 汇总行）；采用行的目标值经 `numbers_add.py` 进 `numbers.md`；"放宽了 Cxx"的行同步进 `issues.md`（`issue_add.py`，类别=`约束被软化`）
- **30 秒人工判定**：看第 1 行是否为 `DEFAULT（未调参）`；行数是否 ≥ 你记得的运行次数（只有 1–2 行即漏记）；`采用` 行恰有一行；抽 1 行的"耗时"与对应 `result.json` 的 `runtime_s` 对得上
- **已知失效模式**：① **只记最好那次** → 幸存者偏差，明天另一班/接手人照论文参数复算不出，V0-3 与 V2-1 双双挂（本卡存在的唯一理由就是防它）② 把"约束不满足"的整行删掉，掩盖放宽事实（等同卡 3-1 三不第 1 条）③ "结论"列写"效果较好"无阈值 ④ 论文引用了未台账化的行，V3-1 数字台账引用率掉下来 ⑤ 手填好看的耗时，与 result.json 不一致 ⑥ 把调参过程当"敏感性分析"写进正文，评委追问时拿不出系统扫描证据
- **验证状态**：未压测
